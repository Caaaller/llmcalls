# Feature flags

# Enable sentence-level streaming TTS. When true, the AI's JSON response is
# streamed and each complete sentence is dispatched to Telnyx TTS as soon as
# the "speech" field delimiter is seen — cuts first-audio latency at the cost
# of more speak.ended webhooks per turn. Default: false (single-blob TTS).
USE_STREAMING=false
