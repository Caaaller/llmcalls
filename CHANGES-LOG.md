# Changes Log — Rolling Fix Tracker

Running table of behavior/bugfix changes across sessions. Read this when
something seems off — it may already be documented, or still open. Append a
new row for every user-visible behavior change; drop rows once they've been
in production long enough to be stable (~30 days).

For latency-specific work, see [`LATENCY-OPTIMIZATIONS.md`](./LATENCY-OPTIMIZATIONS.md).

---

## ✅ Shipped (on main)

| #   | Date       | Area                   | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Commit              | Test coverage                                            |
| --- | ---------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------- |
| 1   | 2026-04-23 | TTS — digit cadence    | Streaming path now spaces multi-digit numbers so TTS reads them "seven two zero" not "seven hundred twenty". Non-streaming path already did this — bug was introduced when streaming became default on `f3a377d`. Extracted `spaceOutNumbers` to `utils/` for testability.                                                                                                                                                                                                                                                         | `26f5e48` + earlier | **12 unit tests** in `spaceOutNumbers.test.ts`           |
| 2   | 2026-04-23 | UI — transcript        | `turn_timing` diagnostic events were rendering as empty rows in HistoryTab. Filtered them out of the timeline.                                                                                                                                                                                                                                                                                                                                                                                                                     | `26f5e48`           | —                                                        |
| 3   | 2026-04-23 | UI — AI timestamp seek | Clicking an AI event timestamp in HistoryTab was seeking the recording ~1s past the audio start. Root cause: conversation/ai event was logged AFTER LLM stream completion. Fix: stamp at `firstSentenceDispatchedAt`.                                                                                                                                                                                                                                                                                                              | `b1268c6`           | —                                                        |
| 4   | 2026-04-23 | Test harness           | `liveCallEval.test.ts` now POSTs test run status to `http://localhost:8068` instead of through ngrok. Two live tests silently failed to register in Test Runs UI today due to stale `BASE_URL`.                                                                                                                                                                                                                                                                                                                                    | `d246d11`           | —                                                        |
| 5   | 2026-04-23 | Test harness           | Added `pnpm --filter backend verify:testrun` — queries latest testrun, fails if status is in_progress/aborted, testCases empty, or >20min old. MANDATORY after every live test per CLAUDE.md.                                                                                                                                                                                                                                                                                                                                      | `eec786f`           | Script is itself the test                                |
| 6   | 2026-04-23 | Hold detection         | Prompt now recognizes voicebot agent-fetch phrases ("let me find someone", "one moment while I get an agent", "I'll transfer you") as hold signals. AT&T's live call failed because prompt required "please hold" / "stay on the line" literals.                                                                                                                                                                                                                                                                                   | `a909f8c`           | 5 prompt-eval cases in `promptEvaluationService.test.ts` |
| 7   | 2026-04-23 | Hold detection         | Expanded — ALL "one moment / just a moment / hang tight / bear with me" phrases now holdDetected=true when purpose is reaching a rep. Previously bare "one moment please" was considered a non-hold short transition.                                                                                                                                                                                                                                                                                                              | `a01d3b6`           | 3 more prompt-eval cases                                 |
| 8   | 2026-04-23 | Loop override          | When AI returns `detected.loopDetected=true` but chose `wait` or `speak` instead of `press_digit`, the backend now FORCES a press using the first unpressed menu option. Intended for IVRs like Costco that replay a menu continuously with no silence — the AI's "wait" would loop forever.                                                                                                                                                                                                                                       | `a01d3b6`           | Not yet — needs live validation                          |
| 9   | 2026-04-23 | Self-call test harness | Added end-to-end "simulator human agent" test. When an outbound call is placed to a second Telnyx DID (`TELNYX_SIMULATOR_NUMBER`), the webhook detects the inbound leg and auto-answers with a randomized scripted human-rep greeting + confirmation. Validates the AI's maybe_human → confirmation → human_detected → transfer pipeline without relying on real agents. Fixture `self-call-human-greeting` is skipped cleanly when env var unset. Setup docs at [`docs/SELF-CALL-TEST-SETUP.md`](./docs/SELF-CALL-TEST-SETUP.md). | _pending commit_    | **6 unit tests** in `simulatorAgent.test.ts`             |

## 🟡 Known issues (not yet fixed)

| #   | Area                            | Description                                                                                                                                                                                                                                                                                                                                                           | Evidence                                                              | Plan                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A   | AI decision — Best Buy voicebot | AI says "press zero" into a voice-bot line instead of sending DTMF tone or saying "operator". Root cause: prompt's "say digit names when DTMF rejected" fallback fires inappropriately on voice bots (which don't accept DTMF at all, so speaking "zero" is nonsense).                                                                                                | Best Buy call 2026-04-23 15:01:37 ai:"press zero"                     | Teach prompt to distinguish voice-bot (no DTMF menu ever seen) vs DTMF-IVR — only use the digit-name-speak fallback on the latter. On voice bots use "operator" / "representative". Not urgent.                                                                                                                                                                                                        |
| B   | Costco looping menu             | Loop-override from `a01d3b6` assumes the AI sees the menu twice to set `loopDetected=true`. Costco speaks continuously with no silence gap, so Deepgram's `utterance_end` never fires after our first DTMF — the AI never gets a second `speech_final` event, loop never flagged, override never triggers. Validated 2026-04-23 17:11: 1 DTMF + runner_timeout again. | Costco calls 2026-04-23 15:03 and 17:11 → 1 DTMF each, runner_timeout | Need a **deterministic, time-based** loop detector that fires without needing another LLM turn: after a DTMF press, watch Deepgram interim transcripts; if interims keep accumulating without crossing the `utterance_end` threshold for N seconds, force-dispatch the current transcript to the AI so it can see the loop. Alternative: shorten `utterance_end_ms` specifically in post-DTMF windows. |
| C   | Test runs UI ordering           | Test Runs tab sorts such that older aborted runs can appear above newer completed ones. Minor — 2 phantom runs from curl probes were deleted, but this could recur.                                                                                                                                                                                                   | UI screenshot 2026-04-23 ~12:15                                       | Change UI sort to `startedAt` desc (or `createdAt`), and add an upstream filter that excludes `status=aborted` runs with empty testCases.                                                                                                                                                                                                                                                              |

## 📝 How to use this doc

- **Before filing a bug**, check the Known Issues table — may already be logged.
- **After shipping a behavior fix**, add a row to "Shipped" with commit hash + test coverage.
- **Prune** rows older than ~30 days that haven't regressed.
- For latency work, this doc defers to `LATENCY-OPTIMIZATIONS.md`.
